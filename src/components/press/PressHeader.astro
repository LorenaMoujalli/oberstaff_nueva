---
import "../../styles/press/press-header.css";

interface Props {
    title: string;
    class?: string;
}

const { title, class: className } = Astro.props;
---

<h2 class={`press-subtitle ${className || ''}`} set:html={title} />
